<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SmartBuild | ERP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root { --sidebar-width: 280px; --bg-dark: #000; --card-bg: #111; }
        
        body { 
            background-color: var(--bg-dark); 
            color: #fff; 
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
        }
        
        /* Sidebar Styling */
        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            position: fixed;
            left: 0; 
            top: 0;
            background-color: var(--card-bg);
            border-right: 1px solid #222;
            padding: 25px 15px;
            z-index: 1050;
            overflow-y: auto;
            transition: transform 0.3s ease;
        }

        /* Dropdown Group Styling */
        .menu-label {
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #444;
            margin: 15px 0 5px 15px;
            font-weight: 800;
        }

        .nav-link { 
            color: #888; 
            padding: 10px 15px; 
            border-radius: 8px; 
            margin-bottom: 4px; 
            transition: 0.2s; 
            display: flex; 
            align-items: center; 
            text-decoration: none;
            font-size: 0.9rem;
        }
        
        .nav-link:hover, .nav-link.active { 
            background: #1a1a1a; 
            color: #0d6efd; 
        }

        .nav-link.collapsed i.fa-chevron-down {
            transform: rotate(-90deg);
        }

        .nav-link i.fa-chevron-down {
            font-size: 0.7rem;
            transition: transform 0.3s;
        }
        
        .nav-link i:first-child { width: 25px; font-size: 1rem; }

        /* Dropdown Inner Links */
        .collapse-inner {
            padding-left: 20px;
            margin-bottom: 10px;
            border-left: 1px solid #222;
            margin-left: 22px;
        }

        .collapse-inner .nav-link {
            font-size: 0.85rem;
            padding: 8px 15px;
        }

        /* Consistent User Info Item */
        .user-section {
            margin-top: auto;
            padding-top: 20px;
            border-top: 1px solid #222;
        }
        .user-info-item {
            color: #fff;
            padding: 12px 15px;
            display: flex;
            align-items: center;
        }
        .user-info-item i { width: 30px; font-size: 1.5rem; color: #0d6efd; }

        /* Main Content Styling */
        .main-content {
            margin-left: var(--sidebar-width);
            width: calc(100% - var(--sidebar-width));
            padding: 40px;
            min-height: 100vh;
            position: relative;
            transition: margin 0.3s ease, width 0.3s ease;
        }

        @media (max-width: 992px) {
            .sidebar { transform: translateX(-100%); }
            .sidebar.active { transform: translateX(0); }
            .main-content { margin-left: 0; width: 100%; padding: 20px; padding-top: 80px; }
            .sidebar-overlay {
                position: fixed; top: 0; left: 0; width: 100%; height: 100%;
                background: rgba(0,0,0,0.7); z-index: 1040; display: none;
            }
            .sidebar-overlay.show { display: block; }
        }

        .mobile-header {
            display: none; position: fixed; top: 0; left: 0; right: 0; height: 60px;
            background: var(--card-bg); border-bottom: 1px solid #222; z-index: 1030;
            align-items: center; padding: 0 20px;
        }

        @media (max-width: 992px) { .mobile-header { display: flex; justify-content: space-between; } }

        .auth-container { min-height: 100vh; display: flex; align-items: center; justify-content: center; width: 100%; }
        .card { background-color: var(--card-bg); border: 1px solid #222; border-radius: 12px; }
        .form-control, .form-select { background-color: #000 !important; border: 1px solid #333 !important; color: #fff !important; padding: 12px; }
    </style>
</head>
<body>

    {{#if user}}
    <header class="mobile-header">
        <button class="btn btn-dark border-secondary" onclick="toggleSidebar()">
            <i class="fas fa-bars"></i>
        </button>
        <h5 class="fw-bold text-primary mb-0">SMARTBUILD</h5>
        <div style="width: 40px;"></div> 
    </header>

    <div class="sidebar-overlay" id="sidebarOverlay" onclick="toggleSidebar()"></div>

    <aside class="sidebar d-flex flex-column" id="sidebarMenu">
        <div class="d-flex justify-content-between align-items-center mb-4 px-2">
            <div>
                <h3 class="fw-bold text-primary mb-0">SMARTBUILD</h3>
                <small class="text-secondary text-uppercase tracking-widest" style="font-size: 0.6rem;">Construction ERP</small>
            </div>
            <button class="btn d-lg-none text-secondary" onclick="toggleSidebar()">
                <i class="fas fa-times"></i>
            </button>
        </div>

        <nav class="nav flex-column mb-auto">
            
            <div class="menu-label">Analytics</div>
            <a class="nav-link justify-content-between" data-bs-toggle="collapse" href="#menuBI" role="button">
                <span><i class="fas fa-chart-line"></i> Business Intelligence</span>
                <i class="fas fa-chevron-down"></i>
            </a>
            <div class="collapse show" id="menuBI">
                <div class="collapse-inner">
                    <a class="nav-link" href="/"><i class="fas fa-th-large"></i> Dashboard</a>
                </div>
            </div>

            <div class="menu-label">Operational</div>
            <a class="nav-link justify-content-between collapsed" data-bs-toggle="collapse" href="#menuOps" role="button">
                <span><i class="fas fa-hard-hat"></i> Operations</span>
                <i class="fas fa-chevron-down"></i>
            </a>
            <div class="collapse" id="menuOps">
                <div class="collapse-inner">
                    <a class="nav-link" href="/projects"><i class="fas fa-tasks"></i> Projects</a>
                    <a class="nav-link" href="/expenses"><i class="fas fa-receipt"></i> Daily Expenses</a>
                    <a class="nav-link" href="/invoices"><i class="fas fa-file-invoice"></i> Proforma Invoices</a>
                </div>
            </div>

            <div class="menu-label">Taxation</div>
            <a class="nav-link justify-content-between collapsed" data-bs-toggle="collapse" href="#menuTax" role="button">
                <span><i class="fas fa-university"></i> RRA Compliances</span>
                <i class="fas fa-chevron-down"></i>
            </a>
            <div class="collapse" id="menuTax">
                <div class="collapse-inner">
                    <a class="nav-link" href="/purchases"><i class="fas fa-file-download"></i> Purchases</a>
                    <a class="nav-link" href="/rra-sales"><i class="fas fa-file-upload"></i> Sales</a>
                    <a class="nav-link text-info" href="/reports/vat-summary"><i class="fas fa-chart-bar"></i> VAT Summary</a>
                </div>
            </div>

            <div class="menu-label">Management</div>
            <a class="nav-link justify-content-between collapsed" data-bs-toggle="collapse" href="#menuInternal" role="button">
                <span><i class="fas fa-shield-alt"></i> Internal Control</span>
                <i class="fas fa-chevron-down"></i>
            </a>
            <div class="collapse" id="menuInternal">
                <div class="collapse-inner">
                    <a class="nav-link" href="/loans"><i class="fas fa-hand-holding-usd"></i> Loans</a>
                    <a class="nav-link" href="/audit"><i class="fas fa-history"></i> Audit Logs</a>
                </div>
            </div>

        </nav>

        <div class="user-section">
            <div class="user-info-item">
                <i class="fas fa-user-circle"></i>
                <div class="user-details ms-2">
                    <div class="small fw-bold">{{user.name}}</div>
                    <div class="text-warning x-small uppercase tracking-wider" style="font-size: 0.65rem;">{{user.role}}</div>
                </div>
            </div>
            <a class="nav-link text-danger mt-1" href="/auth/logout">
                <i class="fas fa-power-off"></i> Logout
            </a>
        </div>
    </aside>
    {{/if}}

    <main class="{{#if user}}main-content{{else}}auth-container{{/if}}">
        {{{body}}}
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebarMenu');
            const overlay = document.getElementById('sidebarOverlay');
            if(sidebar) sidebar.classList.toggle('active');
            if(overlay) overlay.classList.toggle('show');
        }
        
        // Auto-close sidebar on mobile when a sub-link is clicked
        document.querySelectorAll('.collapse-inner .nav-link').forEach(link => {
            link.addEventListener('click', () => {
                if(window.innerWidth <= 992) toggleSidebar();
            });
        });
    </script>
</body>
</html>